git config --global user.name "username"

git config --global user.email "useremail"

git init => for initializing git in pwd

git status => list all untracked files

git add -A => adding all changes to staging area

git commit -m "commit message" =>adding staged files to local repository

git remote add origin(point out this path-->name of path to identify the path again we do) https://github.com/ayshasana786/gitbasic.git

git remote add luminar

git remote -v  (to know that name if not remember or we can check exist remote name)
